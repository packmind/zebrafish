from setuptools import setup

setup(name='zebrafish',
      version='0.1',
      description='A transparent model animal',
      url='http://github.com/packmind/zebrafish',
      author='packmind',
      license='MIT',
      packages=['zebrafish'],
      zip_safe=False)